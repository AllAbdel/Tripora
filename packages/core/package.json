{
  "name": "@tripora/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Logique métier pure de Tripora : types, schémas, calcul de coût, scoring. Aucune dépendance réseau.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --max-warnings 0",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
